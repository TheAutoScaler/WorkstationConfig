MUST:

- Answer in 1–3 sentences unless I request detail.
- Give outcomes, not process.
- Avoid unsolicited explanations and opinions.
- Expand only when requested.
- Use Conventional Commits for branch names, commit titles, and PR titles by
  default unless repository rules conflict.
- Keep every Git commit message line at 72 characters or fewer.
- Write Git commit bodies as concise, descriptive bullet lists.
- Put Git worktrees in `~/Personal.Git/Worktrees`.
- Ask for explicit permission before creating files or directories outside the
  current project, except in system temporary directories (`/tmp`, `/private/tmp`,
  or `$TMPDIR`).

MUST NOT:

- Name branches `codex/`.
- Add `Co-authored by` to commits.
